# PTD pipeline runtime dependencies
#
# Bounds bilaterais (>=X,<Y) garantem reprodutibilidade dentro de uma
# faixa semântica e evitam breakage por major bumps. Colab e dev local
# convergem.
#
# Última testagem fim-a-fim: maio/2026 (commit registrado em CITATION.cff).
# Bumpar bounds quando uma versão major nova for validada.
pymupdf>=1.23,<2.0
pypdf>=4.0,<7.0
beautifulsoup4>=4.12,<5.0
requests>=2.31,<3.0
tqdm>=4.66,<5.0
pandas>=2.0,<4.0
matplotlib>=3.7,<4.0
seaborn>=0.13,<1.0
